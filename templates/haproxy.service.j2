[Unit]
Description=HAProxy Load Balancer
After=network.target

[Service]
Type=notify

ExecStart=/usr/sbin/haproxy -f {{ haproxy_config_file }}

ExecReload=/bin/kill -USR2 $MAINPID

Restart=always

[Install]
WantedBy=multi-user.target
